# Archival

Archive only a stable public release.

Required before archive: tag, commit SHA, paths, checksums, citation metadata and repository origin.

External archive identifiers should be added only after archive confirmation.
