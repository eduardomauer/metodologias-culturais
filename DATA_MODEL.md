# Data Model

Public records should use these minimum fields: title, entity, author, source, date, version, path, licence, access layer and review status.

Release records should add commit SHA, tag and checksum.
